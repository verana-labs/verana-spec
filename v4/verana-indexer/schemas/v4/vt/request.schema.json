{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://verana.io/schemas/v4/vt/request.schema.json",
  "title": "ResolutionRequest",
  "description": "Request payload for the verana indexer DID Resolver. The point-in-time is selected via the `At-Block-Height` HTTP request header (see the indexer spec Conventions section) and is therefore not part of this body. Selectors with object form (`participations`, `presentations`, `ecosystems`) accept `false` to exclude, `true` for defaults, or an object of fine-grained options. See `### Example resolution request` in the indexer spec.",
  "type": "object",
  "additionalProperties": false,
  "required": ["did"],
  "properties": {
    "did": {
      "type": "string",
      "pattern": "^did:",
      "description": "DID to resolve. Any DID method supported by the resolver."
    },

    "corporation": {
      "type": "boolean",
      "default": false,
      "description": "Include the `corporation` object (the unique Corporation whose `did` equals the resolved DID; carries `id`, `deposit`, slash history, and `cgf`). Per VPR a DID is the `did` of at most one Corporation, so this is a singular object rather than an array. Omit or `false` to exclude. The top-level `corporationId` scalar (which equals `corporation.id` whenever the Corporation exists — by VPR's per-Corporation `did` uniqueness invariant they are necessarily the same) is part of the trust-core fields and is always returned regardless of this selector; this selector only controls whether the full Corporation object (`deposit`, slash history, CGF) is also surfaced inline."
    },
    "ecsCredentials": {
      "type": "boolean",
      "default": false,
      "description": "Include the full ECS credentials with subject claims. Omit or `false` to exclude."
    },
    "services": {
      "type": "boolean",
      "default": false,
      "description": "Include `services[]` — non-LinkedVerifiablePresentation service entries from the DID Document (e.g. DIDComm, MCP, LinkedDomains). Omit or `false` to exclude."
    },

    "participations": {
      "description": "Include `participations[]`. `false` to exclude; `true` is sugar for `{}` (defaults: states = [ACTIVE]); object form lets you filter by state.",
      "oneOf": [
        { "type": "boolean" },
        { "$ref": "#/$defs/ParticipationsOptions" }
      ]
    },
    "presentations": {
      "description": "Include `presentations[]`. Each entry always carries `vtcCredentialIds[]`; the sub-flags additionally surface unresolvable / invalid credential IDs per VP.",
      "oneOf": [
        { "type": "boolean" },
        { "$ref": "#/$defs/PresentationsOptions" }
      ]
    },
    "ecosystems": {
      "description": "Include `ecosystems[]`. Sub-flags control whether archived ecosystems (and their archived embedded schemas) are surfaced.",
      "oneOf": [
        { "type": "boolean" },
        { "$ref": "#/$defs/EcosystemsOptions" }
      ]
    }
  },

  "$defs": {
    "ParticipationState": {
      "enum": ["ACTIVE", "FUTURE", "INACTIVE", "EXPIRED", "REVOKED", "SLASHED", "REPAID"]
    },
    "ParticipationsOptions": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "states": {
          "type": "array",
          "uniqueItems": true,
          "default": ["ACTIVE"],
          "items": { "$ref": "#/$defs/ParticipationState" },
          "description": "Filter participations by state. Defaults to [\"ACTIVE\"] when omitted."
        }
      }
    },
    "PresentationsOptions": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "unresolvableCredentialIds": {
          "type": "boolean",
          "default": false,
          "description": "Add `unresolvableCredentialIds[]` to each presentation entry."
        },
        "invalidCredentialIds": {
          "type": "boolean",
          "default": false,
          "description": "Add `invalidCredentialIds[]` to each presentation entry."
        }
      }
    },
    "EcosystemsOptions": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "includeArchived": {
          "type": "boolean",
          "default": false,
          "description": "When true, also include archived ecosystems in the response."
        },
        "credentialSchemas": {
          "description": "Embedded Credential Schemas selector. `false` (or omit) to suppress; `true` to include with defaults; object form for fine-grained control.",
          "oneOf": [
            { "type": "boolean" },
            {
              "type": "object",
              "additionalProperties": false,
              "properties": {
                "includeArchived": {
                  "type": "boolean",
                  "default": false,
                  "description": "When true, also include archived Credential Schemas inside each ecosystem."
                }
              }
            }
          ]
        }
      }
    }
  }
}
